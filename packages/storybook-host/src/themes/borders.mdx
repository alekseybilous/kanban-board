import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="packages/themes/tokens/Borders" />

# Border System

Our border system provides consistent edge styling and corner rounding for all UI components, creating visual separation and defining interactive boundaries.

## 🎯 Border Radius Scale

Interactive showcase of our border radius tokens with live component previews.

<div style={{
  background: 'linear-gradient(135deg, var(--surface) 0%, var(--surface-subtle) 100%)',
  borderRadius: '12px',
  padding: 'var(--space-8)',
  marginBottom: 'var(--space-8)',
  border: '1px solid var(--border)'
}}>
  <div style={{ marginBottom: 'var(--space-6)', textAlign: 'center' }}>
    <h2 style={{ margin: 0, color: 'var(--text)', fontSize: '1.5rem' }}>Border Radius Playground</h2>
    <p style={{ margin: 'var(--space-2) 0 0', color: 'var(--text-secondary)', fontSize: '0.9rem' }}>
      Click any example to copy its CSS variable
    </p>
  </div>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
    gap: 'var(--space-4)'
  }}>
    {[
      { token: 'border-radius-none', value: '0', px: '0px', name: 'None', usage: 'Sharp edges, technical UI' },
      { token: 'border-radius-xs', value: '0.0625rem', px: '1px', name: 'Extra Small', usage: 'Minimal rounding' },
      { token: 'border-radius-sm', value: '0.125rem', px: '2px', name: 'Small', usage: 'Subtle edges' },
      { token: 'border-radius-md', value: '0.25rem', px: '4px', name: 'Medium', usage: 'Standard buttons' },
      { token: 'border-radius-lg', value: '0.375rem', px: '6px', name: 'Large', usage: 'Cards, inputs' },
      { token: 'border-radius-xl', value: '0.5rem', px: '8px', name: 'Extra Large', usage: 'Panels, modals' },
      { token: 'border-radius-2xl', value: '0.75rem', px: '12px', name: '2X Large', usage: 'Hero sections' },
      { token: 'border-radius-3xl', value: '1rem', px: '16px', name: '3X Large', usage: 'Feature cards' },
      { token: 'border-radius-4xl', value: '1.5rem', px: '24px', name: '4X Large', usage: 'Large containers' },
      { token: 'border-radius-full', value: '9999px', px: '∞', name: 'Full', usage: 'Circles, pills' }
    ].map((radius, index) => (
      <div key={radius.token} style={{
        background: 'var(--surface)',
        border: '1px solid var(--border-subtle)',
        borderRadius: '8px',
        padding: 'var(--space-4)',
        cursor: 'pointer',
        transition: 'all 0.2s ease'
      }}
      onClick={() => navigator.clipboard?.writeText(`var(--${radius.token})`)}
      onMouseEnter={(e) => {
        e.target.style.transform = 'translateY(-2px)';
        e.target.style.borderColor = 'var(--primary-border)';
        e.target.style.backgroundColor = 'var(--surface-hover)';
      }}
      onMouseLeave={(e) => {
        e.target.style.transform = 'translateY(0)';
        e.target.style.borderColor = 'var(--border-subtle)';
        e.target.style.backgroundColor = 'var(--surface)';
      }}
      title={`Click to copy var(--${radius.token})`}
      >
        <div style={{
          width: '80px',
          height: '80px',
          background: `hsl(${200 + index * 15}, 65%, 55%)`,
          borderRadius: `var(--${radius.token})`,
          marginBottom: 'var(--space-3)',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          color: 'white',
          fontWeight: '600',
          fontSize: '0.8rem',
          margin: '0 auto var(--space-3) auto'
        }}>
          {radius.px}
        </div>

        <div style={{
          fontSize: 'var(--font-size-sm)',
          fontWeight: 'var(--font-weight-semibold)',
          color: 'var(--text)',
          marginBottom: 'var(--space-1)',
          textAlign: 'center'
        }}>
          {radius.name}
        </div>

        <div style={{
          fontSize: 'var(--font-size-xs)',
          fontFamily: 'monospace',
          color: 'var(--primary-base)',
          marginBottom: 'var(--space-2)',
          textAlign: 'center'
        }}>
          --{radius.token.replace('border-radius-', '')}
        </div>

        <div style={{
          fontSize: 'var(--font-size-xs)',
          color: 'var(--text-secondary)',
          textAlign: 'center',
          lineHeight: '1.3'
        }}>
          {radius.usage}
        </div>
      </div>
    ))}

  </div>
</div>

## 📏 Border Width Scale

Visual demonstration of border thickness options.

<div style={{
  background: 'var(--surface)',
  border: '1px solid var(--border)',
  borderRadius: '8px',
  padding: 'var(--space-6)',
  marginBottom: 'var(--space-8)'
}}>
  <h3 style={{ marginBottom: 'var(--space-4)', color: 'var(--text)' }}>Border Width Variations</h3>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))',
    gap: 'var(--space-4)'
  }}>
    {[
      { token: 'border-width-0', value: '0', name: 'None', usage: 'No border' },
      { token: 'border-width-1', value: '1px', name: 'Thin', usage: 'Default borders' },
      { token: 'border-width-2', value: '2px', name: 'Medium', usage: 'Emphasized borders' },
      { token: 'border-width-4', value: '4px', name: 'Thick', usage: 'Strong emphasis' },
      { token: 'border-width-8', value: '8px', name: 'Extra Thick', usage: 'Heavy emphasis' }
    ].map((width, index) => (
      <div key={width.token} style={{
        background: 'var(--surface-subtle)',
        borderRadius: '6px',
        padding: 'var(--space-4)',
        cursor: 'pointer',
        transition: 'all 0.2s ease'
      }}
      onClick={() => navigator.clipboard?.writeText(`var(--${width.token})`)}
      onMouseEnter={(e) => {
        e.target.style.transform = 'scale(1.02)';
        e.target.style.backgroundColor = 'var(--surface-hover)';
      }}
      onMouseLeave={(e) => {
        e.target.style.transform = 'scale(1)';
        e.target.style.backgroundColor = 'var(--surface-subtle)';
      }}
      title={`Click to copy var(--${width.token})`}
      >
        <div style={{
          width: '100px',
          height: '60px',
          background: 'var(--surface)',
          border: `var(--${width.token}) solid var(--primary-base)`,
          borderRadius: '4px',
          marginBottom: 'var(--space-3)',
          margin: '0 auto var(--space-3) auto',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          fontSize: '0.8rem',
          color: 'var(--text-secondary)'
        }}>
          {width.value === '0' ? 'No Border' : width.value}
        </div>

        <div style={{
          fontSize: 'var(--font-size-sm)',
          fontWeight: 'var(--font-weight-medium)',
          color: 'var(--text)',
          marginBottom: 'var(--space-1)',
          textAlign: 'center'
        }}>
          {width.name}
        </div>

        <div style={{
          fontSize: 'var(--font-size-xs)',
          fontFamily: 'monospace',
          color: 'var(--primary-base)',
          marginBottom: 'var(--space-1)',
          textAlign: 'center'
        }}>
          --{width.token.replace('border-width-', '')}
        </div>

        <div style={{
          fontSize: 'var(--font-size-xs)',
          color: 'var(--text-secondary)',
          textAlign: 'center'
        }}>
          {width.usage}
        </div>
      </div>
    ))}

  </div>
</div>

## 🎨 Real-World Examples

See how borders work in actual UI components.

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
  gap: 'var(--space-6)',
  marginBottom: 'var(--space-8)'
}}>
  {/* Button Examples */}
  <div style={{
    background: 'var(--surface)',
    border: '1px solid var(--border)',
    borderRadius: 'var(--border-radius-lg)',
    padding: 'var(--space-6)'
  }}>
    <h4 style={{
      margin: '0 0 var(--space-4) 0',
      color: 'var(--text)',
      fontSize: 'var(--font-size-lg)'
    }}>
      Button Variants
    </h4>

    <div style={{
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }}>
      {[
        { style: 'Primary', bg: 'var(--primary-base)', color: 'white', radius: 'border-radius-md', border: 'none' },
        { style: 'Secondary', bg: 'transparent', color: 'var(--primary-base)', radius: 'border-radius-md', border: '1px solid var(--primary-border)' },
        { style: 'Pill', bg: 'var(--success-base)', color: 'white', radius: 'border-radius-full', border: 'none' },
        { style: 'Sharp', bg: 'var(--surface-subtle)', color: 'var(--text)', radius: 'border-radius-none', border: '1px solid var(--border)' }
      ].map((btn) => (
        <button key={btn.style} style={{
          background: btn.bg,
          color: btn.color,
          border: btn.border,
          borderRadius: `var(--${btn.radius})`,
          padding: 'var(--space-3) var(--space-4)',
          cursor: 'pointer',
          fontSize: 'var(--font-size-sm)',
          fontWeight: 'var(--font-weight-medium)',
          transition: 'all 0.2s ease'
        }}
        onClick={() => navigator.clipboard?.writeText(`border-radius: var(--${btn.radius})`)}
        title={`Click to copy border-radius: var(--${btn.radius})`}
        onMouseEnter={(e) => {
          e.target.style.transform = 'scale(1.02)';
          e.target.style.opacity = '0.9';
        }}
        onMouseLeave={(e) => {
          e.target.style.transform = 'scale(1)';
          e.target.style.opacity = '1';
        }}
        >
          {btn.style} Button
        </button>
      ))}
    </div>

  </div>

{/* Card Examples */}

  <div style={{
    background: 'var(--surface)',
    border: '1px solid var(--border)',
    borderRadius: 'var(--border-radius-lg)',
    padding: 'var(--space-6)'
  }}>
    <h4 style={{
      margin: '0 0 var(--space-4) 0',
      color: 'var(--text)',
      fontSize: 'var(--font-size-lg)'
    }}>
      Card Styles
    </h4>

    <div style={{
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-4)'
    }}>
      {[
        { name: 'Standard Card', radius: 'border-radius-lg', border: '1px solid var(--border)' },
        { name: 'Rounded Card', radius: 'border-radius-2xl', border: '1px solid var(--border-subtle)' },
        { name: 'Sharp Card', radius: 'border-radius-none', border: '2px solid var(--primary-border)' }
      ].map((card) => (
        <div key={card.name} style={{
          background: 'var(--surface-subtle)',
          border: card.border,
          borderRadius: `var(--${card.radius})`,
          padding: 'var(--space-4)',
          cursor: 'pointer',
          transition: 'all 0.2s ease'
        }}
        onClick={() => navigator.clipboard?.writeText(`border-radius: var(--${card.radius})`)}
        title={`Click to copy border-radius: var(--${card.radius})`}
        onMouseEnter={(e) => {
          e.target.style.transform = 'translateY(-1px)';
          e.target.style.backgroundColor = 'var(--surface-hover)';
        }}
        onMouseLeave={(e) => {
          e.target.style.transform = 'translateY(0)';
          e.target.style.backgroundColor = 'var(--surface-subtle)';
        }}
        >
          <div style={{
            fontSize: 'var(--font-size-sm)',
            fontWeight: 'var(--font-weight-medium)',
            color: 'var(--text)',
            marginBottom: 'var(--space-1)'
          }}>
            {card.name}
          </div>
          <div style={{
            fontSize: 'var(--font-size-xs)',
            fontFamily: 'monospace',
            color: 'var(--text-tertiary)'
          }}>
            --{card.radius}
          </div>
        </div>
      ))}
    </div>

  </div>

{/* Input Examples */}

  <div style={{
    background: 'var(--surface)',
    border: '1px solid var(--border)',
    borderRadius: 'var(--border-radius-lg)',
    padding: 'var(--space-6)'
  }}>
    <h4 style={{
      margin: '0 0 var(--space-4) 0',
      color: 'var(--text)',
      fontSize: 'var(--font-size-lg)'
    }}>
      Input Fields
    </h4>

    <div style={{
      display: 'flex',
      flexDirection: 'column',
      gap: 'var(--space-3)'
    }}>
      {[
        { type: 'Default', radius: 'border-radius-md', border: '1px solid var(--border)', state: 'default' },
        { type: 'Focused', radius: 'border-radius-md', border: '2px solid var(--primary-border)', state: 'focused' },
        { type: 'Error', radius: 'border-radius-md', border: '2px solid var(--danger-border)', state: 'error' },
        { type: 'Rounded', radius: 'border-radius-full', border: '1px solid var(--border)', state: 'rounded' }
      ].map((input) => (
        <div key={input.type} style={{
          display: 'flex',
          flexDirection: 'column',
          gap: 'var(--space-1)'
        }}>
          <label style={{
            fontSize: 'var(--font-size-xs)',
            fontWeight: 'var(--font-weight-medium)',
            color: 'var(--text-secondary)'
          }}>
            {input.type} Input
          </label>
          <input
            type="text"
            placeholder="Enter text..."
            style={{
              background: 'var(--surface)',
              border: input.border,
              borderRadius: `var(--${input.radius})`,
              padding: 'var(--space-3)',
              fontSize: 'var(--font-size-sm)',
              color: 'var(--text)',
              outline: 'none',
              cursor: 'pointer',
              transition: 'all 0.2s ease'
            }}
            onClick={() => navigator.clipboard?.writeText(`border-radius: var(--${input.radius})`)}
            title={`Click to copy border-radius: var(--${input.radius})`}
            onMouseEnter={(e) => {
              e.target.style.backgroundColor = 'var(--surface-hover)';
            }}
            onMouseLeave={(e) => {
              e.target.style.backgroundColor = 'var(--surface)';
            }}
          />
        </div>
      ))}
    </div>

  </div>
</div>

## 🔄 Border Combinations

Explore how borders work with different combinations.

<div style={{
  background: 'var(--surface-subtle)',
  borderRadius: '8px',
  padding: 'var(--space-6)',
  marginBottom: 'var(--space-8)'
}}>
  <h3 style={{ marginBottom: 'var(--space-4)', color: 'var(--text)' }}>Border Style Matrix</h3>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(120px, 1fr))',
    gap: 'var(--space-4)'
  }}>
    {[
      { radius: 'border-radius-sm', width: 'border-width-1', name: 'Subtle' },
      { radius: 'border-radius-md', width: 'border-width-1', name: 'Standard' },
      { radius: 'border-radius-lg', width: 'border-width-2', name: 'Prominent' },
      { radius: 'border-radius-xl', width: 'border-width-1', name: 'Rounded' },
      { radius: 'border-radius-2xl', width: 'border-width-2', name: 'Soft' },
      { radius: 'border-radius-full', width: 'border-width-4', name: 'Pill' },
      { radius: 'border-radius-none', width: 'border-width-2', name: 'Sharp' },
      { radius: 'border-radius-3xl', width: 'border-width-1', name: 'Smooth' }
    ].map((combo, index) => (
      <div key={combo.name} style={{
        background: 'var(--surface)',
        border: `var(--${combo.width}) solid hsl(${180 + index * 25}, 60%, 50%)`,
        borderRadius: `var(--${combo.radius})`,
        padding: 'var(--space-4)',
        cursor: 'pointer',
        transition: 'all 0.2s ease',
        textAlign: 'center'
      }}
      onClick={() => navigator.clipboard?.writeText(`border: var(--${combo.width}) solid var(--border);\nborder-radius: var(--${combo.radius});`)}
      title={`Click to copy combined border styles`}
      onMouseEnter={(e) => {
        e.target.style.transform = 'scale(1.05)';
        e.target.style.backgroundColor = 'var(--surface-hover)';
      }}
      onMouseLeave={(e) => {
        e.target.style.transform = 'scale(1)';
        e.target.style.backgroundColor = 'var(--surface)';
      }}
      >
        <div style={{
          fontSize: 'var(--font-size-sm)',
          fontWeight: 'var(--font-weight-medium)',
          color: 'var(--text)',
          marginBottom: 'var(--space-2)'
        }}>
          {combo.name}
        </div>
        <div style={{
          fontSize: 'var(--font-size-xs)',
          fontFamily: 'monospace',
          color: 'var(--text-tertiary)',
          lineHeight: '1.3'
        }}>
          {combo.radius.replace('border-radius-', '')}<br/>
          {combo.width.replace('border-width-', '')}
        </div>
      </div>
    ))}
  </div>
</div>

## 🎭 Component Showcase

Real-world component examples using different border patterns.

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))',
  gap: 'var(--space-6)',
  marginBottom: 'var(--space-8)'
}}>
  {/* Profile Card */}
  <div style={{
    background: 'var(--surface)',
    border: '1px solid var(--border)',
    borderRadius: 'var(--border-radius-2xl)',
    padding: 'var(--space-6)',
    textAlign: 'center',
    cursor: 'pointer',
    transition: 'transform 0.2s ease'
  }}
  onMouseEnter={(e) => e.target.style.transform = 'translateY(-4px)'}
  onMouseLeave={(e) => e.target.style.transform = 'translateY(0)'}
  >
    <div style={{
      width: '80px',
      height: '80px',
      background: 'var(--primary-base)',
      borderRadius: 'var(--border-radius-full)',
      margin: '0 auto var(--space-4) auto',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'white',
      fontSize: 'var(--font-size-2xl)',
      fontWeight: 'var(--font-weight-bold)'
    }}>
      JD
    </div>
    <h3 style={{
      margin: '0 0 var(--space-1) 0',
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-semibold)',
      color: 'var(--text)'
    }}>
      John Doe
    </h3>
    <p style={{
      margin: '0 0 var(--space-4) 0',
      fontSize: 'var(--font-size-sm)',
      color: 'var(--text-secondary)'
    }}>
      Product Designer
    </p>
    <button style={{
      background: 'var(--primary-base)',
      color: 'white',
      border: 'none',
      borderRadius: 'var(--border-radius-full)',
      padding: 'var(--space-2) var(--space-6)',
      fontSize: 'var(--font-size-sm)',
      fontWeight: 'var(--font-weight-medium)',
      cursor: 'pointer'
    }}>
      Follow
    </button>
  </div>

{/* Notification */}

{' '}
<div
  style={{
    background: 'var(--warning-surface)',
    border: '2px solid var(--warning-border)',
    borderRadius: 'var(--border-radius-lg)',
    padding: 'var(--space-4)',
    cursor: 'pointer',
    transition: 'transform 0.2s ease',
  }}
  onMouseEnter={(e) => (e.target.style.transform = 'scale(1.02)')}
  onMouseLeave={(e) => (e.target.style.transform = 'scale(1)')}
>
  <div
    style={{
      display: 'flex',
      alignItems: 'flex-start',
      gap: 'var(--space-3)',
    }}
  >
    <div
      style={{
        width: '6px',
        height: '6px',
        background: 'var(--warning-base)',
        borderRadius: 'var(--border-radius-full)',
        marginTop: 'var(--space-1)',
        flexShrink: 0,
      }}
    />
    <div>
      <h4
        style={{
          margin: '0 0 var(--space-2) 0',
          fontSize: 'var(--font-size-sm)',
          fontWeight: 'var(--font-weight-semibold)',
          color: 'var(--warning-text)',
        }}
      >
        System Maintenance
      </h4>
      <p
        style={{
          margin: 0,
          fontSize: 'var(--font-size-sm)',
          color: 'var(--warning-text)',
          lineHeight: 'var(--line-height-normal)',
        }}
      >
        Scheduled maintenance will occur tonight from 2-4 AM EST.
      </p>
    </div>
  </div>
</div>

{/* Feature Card */}

  <div style={{
    background: 'var(--surface)',
    border: '1px solid var(--border)',
    borderRadius: 'var(--border-radius-3xl)',
    padding: 'var(--space-6)',
    cursor: 'pointer',
    transition: 'all 0.2s ease'
  }}
  onMouseEnter={(e) => {
    e.target.style.borderColor = 'var(--primary-border)';
    e.target.style.backgroundColor = 'var(--surface-hover)';
  }}
  onMouseLeave={(e) => {
    e.target.style.borderColor = 'var(--border)';
    e.target.style.backgroundColor = 'var(--surface)';
  }}
  >
    <div style={{
      width: '48px',
      height: '48px',
      background: 'var(--success-surface)',
      border: '2px solid var(--success-border)',
      borderRadius: 'var(--border-radius-xl)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      marginBottom: 'var(--space-4)',
      fontSize: 'var(--font-size-xl)'
    }}>
      🚀
    </div>
    <h3 style={{
      margin: '0 0 var(--space-2) 0',
      fontSize: 'var(--font-size-lg)',
      fontWeight: 'var(--font-weight-semibold)',
      color: 'var(--text)'
    }}>
      Fast Performance
    </h3>
    <p style={{
      margin: 0,
      fontSize: 'var(--font-size-sm)',
      color: 'var(--text-secondary)',
      lineHeight: 'var(--line-height-normal)'
    }}>
      Lightning-fast loading times with optimized rendering and caching.
    </p>
  </div>
</div>

## 💡 Best Practices

<div style={{
  display: 'grid',
  gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
  gap: 'var(--space-6)',
  marginBottom: 'var(--space-8)'
}}>
  <div style={{
    background: 'var(--success-surface)',
    border: '1px solid var(--success-border)',
    borderRadius: '8px',
    padding: 'var(--space-4)'
  }}>
    <h4 style={{
      color: 'var(--success-text)',
      marginBottom: 'var(--space-3)',
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-2)'
    }}>
      <span>✅</span> Do's
    </h4>
    <ul style={{
      margin: 0,
      paddingLeft: 'var(--space-4)',
      color: 'var(--success-text)',
      fontSize: 'var(--font-size-sm)',
      lineHeight: 'var(--line-height-normal)'
    }}>
      <li>Use consistent border radius across similar components</li>
      <li>Match border radius to your brand's visual style</li>
      <li>Consider the component's size when choosing radius</li>
      <li>Use subtle borders for clean, minimal designs</li>
      <li>Test borders across different themes and backgrounds</li>
    </ul>
  </div>

  <div style={{
    background: 'var(--danger-surface)',
    border: '1px solid var(--danger-border)',
    borderRadius: '8px',
    padding: 'var(--space-4)'
  }}>
    <h4 style={{
      color: 'var(--danger-text)',
      marginBottom: 'var(--space-3)',
      display: 'flex',
      alignItems: 'center',
      gap: 'var(--space-2)'
    }}>
      <span>❌</span> Don'ts
    </h4>
    <ul style={{
      margin: 0,
      paddingLeft: 'var(--space-4)',
      color: 'var(--danger-text)',
      fontSize: 'var(--font-size-sm)',
      lineHeight: 'var(--line-height-normal)'
    }}>
      <li>Mix too many different border styles in one interface</li>
      <li>Use extremely thick borders for small components</li>
      <li>Apply full border radius to rectangular content</li>
      <li>Forget about border visibility on different backgrounds</li>
      <li>Use arbitrary border values outside the design system</li>
    </ul>
  </div>
</div>

## 🔧 Quick Reference

Common border patterns ready to copy and use.

<div style={{
  background: 'var(--surface-subtle)',
  borderRadius: '8px',
  padding: 'var(--space-4)',
  marginBottom: 'var(--space-8)'
}}>
  <h4 style={{ marginBottom: 'var(--space-3)', color: 'var(--text)' }}>Common Border Patterns</h4>

  <div style={{
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))',
    gap: 'var(--space-3)'
  }}>
    {[
      { name: 'Button', css: 'border: var(--border-width-1) solid var(--border);\nborder-radius: var(--border-radius-md);' },
      { name: 'Card', css: 'border: var(--border-width-1) solid var(--border);\nborder-radius: var(--border-radius-lg);' },
      { name: 'Input', css: 'border: var(--border-width-1) solid var(--border);\nborder-radius: var(--border-radius-md);' },
      { name: 'Badge', css: 'border: var(--border-width-1) solid var(--border);\nborder-radius: var(--border-radius-full);' },
      { name: 'Panel', css: 'border: var(--border-width-1) solid var(--border);\nborder-radius: var(--border-radius-xl);' },
      { name: 'Avatar', css: 'border: var(--border-width-2) solid var(--border);\nborder-radius: var(--border-radius-full);' }
    ].map((pattern) => (
      <div key={pattern.name} style={{
        background: 'var(--surface)',
        border: '1px solid var(--border-subtle)',
        borderRadius: '6px',
        padding: 'var(--space-3)',
        cursor: 'pointer',
        transition: 'all 0.2s ease'
      }}
      onClick={() => navigator.clipboard?.writeText(pattern.css)}
      onMouseEnter={(e) => {
        e.target.style.borderColor = 'var(--primary-border)';
        e.target.style.backgroundColor = 'var(--surface-hover)';
      }}
      onMouseLeave={(e) => {
        e.target.style.borderColor = 'var(--border-subtle)';
        e.target.style.backgroundColor = 'var(--surface)';
      }}
      title={`Click to copy CSS for ${pattern.name}`}
      >
        <div style={{
          fontSize: 'var(--font-size-sm)',
          fontWeight: 'var(--font-weight-medium)',
          color: 'var(--text)',
          marginBottom: 'var(--space-1)'
        }}>
          {pattern.name}
        </div>
        <div style={{
          fontSize: 'var(--font-size-xs)',
          fontFamily: 'monospace',
          color: 'var(--text-tertiary)',
          whiteSpace: 'pre-line'
        }}>
          {pattern.css}
        </div>
      </div>
    ))}
  </div>
</div>

---

_Our border system creates visual structure and hierarchy while maintaining consistency across all interface elements._
